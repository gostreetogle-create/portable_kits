# QUICKSTART — auth-rbac-kit

> **v0.1** — working `src/` with permission helpers, Angular service, Express middleware.

```bash
# Demo and tests via schema-table-kit hub:
cd schema-table-kit
npm test          # includes auth-rbac-kit tests
ng serve demo     # → /modules/auth-rbac-kit
```

**Consumer:** copy `auth-rbac-kit/src/` → `packages/auth-rbac-kit/src/`, add path aliases, call `provideAuthRbacKit()`.

See [README.md](./README.md), [COPY-GUIDE.md](./COPY-GUIDE.md).
