# QUICKSTART — photo-uploader-kit

> **v0.1** — working `src/` with photo uploader, gallery, drag-drop, zoom, frame editor.

```bash
# Demo and tests via schema-table-kit hub:
cd schema-table-kit
npm test          # includes photo-uploader-kit tests
ng serve demo     # → /modules/photo-uploader-kit
```

**Consumer:** copy `photo-uploader-kit/src/` → `packages/photo-uploader-kit/src/`, add path aliases, use `<pu-photo-uploader>` + `providePhotoUploaderKit()`.

See [README.md](./README.md), [COPY-GUIDE.md](./COPY-GUIDE.md).
