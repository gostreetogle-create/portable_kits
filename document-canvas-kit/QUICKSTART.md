# QUICKSTART — document-canvas-kit

> **v0.2** — working `src/` with text/header/table/separator blocks + background image.

```bash
# Demo and tests via schema-table-kit hub:
cd schema-table-kit
npm test          # includes document-canvas-kit tests
ng serve demo     # → /modules/document-canvas-kit
```

**Consumer:** copy `document-canvas-kit/src/` → `packages/document-canvas-kit/src/`, add path aliases, use `<dc-document-canvas>`.

See [README.md](./README.md), [COPY-GUIDE.md](./COPY-GUIDE.md).
