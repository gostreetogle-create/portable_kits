# QUICKSTART — entity-picker-kit

> **v0.2** — working `src/` with entity picker dialog (single + multi-select).

```bash
# Demo and tests via schema-table-kit hub:
cd schema-table-kit
npm test          # includes entity-picker-kit tests
ng serve demo     # → /modules/entity-picker-kit
```

**Consumer:** copy `entity-picker-kit/src/` → `packages/entity-picker-kit/src/`, add path aliases, use `<ep-entity-picker>` + `provideEntityPickerKit()`.

See [README.md](./README.md), [COPY-GUIDE.md](./COPY-GUIDE.md).
