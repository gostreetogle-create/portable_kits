# INTEGRATION-KPPDF — crud-factory-kit

**Status:** ✅ v0.1 ready in portable_kits (2026-05-30)

Copy `crud-factory-kit/src/`, wire `createCrudRouter(model, options)` with your Mongoose model + auth.

**KPPDF source:** `backend/src/utils/crud-factory.ts`

**Hub demo:** `/modules/crud-factory-kit` · **Tests:** `schema-table-kit/tests/crud-factory-kit.spec.ts`

Consumer must provide Express + Mongoose (peer deps).
