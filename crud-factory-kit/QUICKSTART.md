# QUICKSTART — crud-factory-kit

> **v0.1** — working `src/` with Express CRUD router + API response helpers.

```bash
# Demo and tests via schema-table-kit hub:
cd schema-table-kit
npm test          # includes crud-factory-kit tests
ng serve demo     # → /modules/crud-factory-kit
```

**Consumer:** copy `crud-factory-kit/src/` → `packages/crud-factory-kit/src/`, add path aliases, use `createCrudRouter(model, options)`.

See [README.md](./README.md), [COPY-GUIDE.md](./COPY-GUIDE.md).
