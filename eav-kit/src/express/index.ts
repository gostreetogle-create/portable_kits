/** Express adapter for eav-kit — reserve for future server-side EAV operations. */
export {};

// TODO: Add EAV Express routes when needed:
// - GET /eav/:entityKey — list attributes
// - PUT /eav/:entityKey/:attributeId — update attribute value
