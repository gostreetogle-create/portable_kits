# QUICKSTART — eav-kit

> **v0.1** — working `src/` with EAV attribute editor component.

```bash
# Demo and tests via schema-table-kit hub:
cd schema-table-kit
npm test          # includes eav-kit tests
ng serve demo     # → /modules/eav-kit
```

**Consumer:** copy `eav-kit/src/` → `packages/eav-kit/src/`, add path aliases, use `<eav-attribute-editor>` + `provideEavKit()`.

See [README.md](./README.md), [COPY-GUIDE.md](./COPY-GUIDE.md).
