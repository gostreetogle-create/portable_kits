# QUICKSTART — options-resolver-kit

> **v0.1** — working `src/` with OptionsResolver service + cache.

```bash
# Demo and tests via schema-table-kit hub:
cd schema-table-kit
npm test          # includes options-resolver-kit tests
ng serve demo     # → /modules/options-resolver-kit
```

**Consumer:** copy `options-resolver-kit/src/` → `packages/options-resolver-kit/src/`, add path aliases, use `OptionsResolver.getOptions()` + `provideOptionsResolver()`.

See [README.md](./README.md), [COPY-GUIDE.md](./COPY-GUIDE.md).
