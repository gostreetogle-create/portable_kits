# QUICKSTART — ui-primeng-kit

> **v0.1** — working `src/` with KpButton, KpInput, KpDialog, KpTable, KpPaginator + 14 more components.

```bash
# Demo and tests via schema-table-kit hub:
cd schema-table-kit
npm test          # includes ui-primeng-kit tests
ng serve demo     # → /modules/ui-primeng-kit
```

**Consumer:** copy `ui-primeng-kit/src/` → `packages/ui-primeng-kit/src/`, add path aliases, install PrimeNG deps, call `provideUiPrimengKit()`.

See [README.md](./README.md), [COPY-GUIDE.md](./COPY-GUIDE.md) (detailed consumer guide inside).
