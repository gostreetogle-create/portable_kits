{
  "name": "schema-data-table-kit",
  "version": "0.1.0",
  "private": true,
  "description": "Portable kit: data table using schema-table config (plain HTML v0.1)",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "~5.9.0",
    "vitest": "^3.0.0"
  }
}
