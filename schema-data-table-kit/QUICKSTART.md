# QUICKSTART — schema-data-table-kit

> **v0.1** — working `src/` with data table component using column config.

```bash
# Demo and tests via schema-table-kit hub:
cd schema-table-kit
npm test          # includes schema-data-table-kit tests
ng serve demo     # → /modules/schema-data-table-kit
```

**Consumer:** copy `schema-data-table-kit/src/` → `packages/schema-data-table-kit/src/`, add path aliases, use `<sdt-schema-data-table>` + `provideSchemaDataTableKit()`.

See [README.md](./README.md), [COPY-GUIDE.md](./COPY-GUIDE.md).
