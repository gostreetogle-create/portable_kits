/** Express adapter for placeholder-kit — reserve for server-side placeholder resolution. */
export {};

// TODO: Add Express placeholder resolution endpoints when needed:
// - POST /placeholders/resolve — resolve placeholders for a given context
