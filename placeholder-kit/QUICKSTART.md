# QUICKSTART — placeholder-kit

> **v0.1** — working `src/` with placeholder resolution engine + picker UI.

```bash
# Demo and tests via schema-table-kit hub:
cd schema-table-kit
npm test          # includes placeholder-kit tests
ng serve demo     # → /modules/placeholder-kit
```

**Consumer:** copy `placeholder-kit/src/` → `packages/placeholder-kit/src/`, add path aliases, use `resolvePlaceholders()` + `<ph-placeholder-picker>` + `providePlaceholderKit()`.

See [README.md](./README.md), [COPY-GUIDE.md](./COPY-GUIDE.md).
