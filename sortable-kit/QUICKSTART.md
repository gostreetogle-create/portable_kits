# QUICKSTART — sortable-kit

> **v0.1** — working `src/` with drag-and-drop directives (Angular CDK).

```bash
# Demo and tests via schema-table-kit hub:
cd schema-table-kit
npm test          # includes sortable-kit tests
ng serve demo     # → /modules/sortable-kit
```

**Consumer:** copy `sortable-kit/src/` → `packages/sortable-kit/src/`, add path aliases, use `soSortableList` + `soSortableItem` directives.

See [README.md](./README.md), [COPY-GUIDE.md](./COPY-GUIDE.md).
