# QUICKSTART — layout-shell-kit

> **v0.1** — working `src/` with sidebar navigation shell + responsive mobile drawer.

```bash
# Demo and tests via schema-table-kit hub:
cd schema-table-kit
npm test          # includes layout-shell-kit tests
ng serve demo     # → /modules/layout-shell-kit
```

**Consumer:** copy `layout-shell-kit/src/` → `packages/layout-shell-kit/src/`, add path aliases, use `<ls-layout-shell>` + `provideLayoutShellKit()`.

See [README.md](./README.md), [COPY-GUIDE.md](./COPY-GUIDE.md).
