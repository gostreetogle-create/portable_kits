# QUICKSTART — crud-page-kit

> **v0.1** — working `src/` with CrudStore + generic list page component.

```bash
# Demo and tests via schema-table-kit hub:
cd schema-table-kit
npm test          # includes crud-page-kit tests
ng serve demo     # → /modules/crud-page-kit
```

**Consumer:** copy `crud-page-kit/src/` → `packages/crud-page-kit/src/`, add path aliases, use `<cp-crud-page>` + `CrudStore`.

See [README.md](./README.md), [COPY-GUIDE.md](./COPY-GUIDE.md).
