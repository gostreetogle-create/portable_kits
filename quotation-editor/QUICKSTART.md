# QUICKSTART — quotation-editor

> **v0.1** — working `src/` composing document-canvas-kit + entity-picker-kit + placeholder-kit.

```bash
# Demo and tests via schema-table-kit hub:
cd schema-table-kit
npm test          # includes quotation-editor tests
ng serve demo     # → /modules/quotation-editor
```

**Consumer:** copy `quotation-editor/src/` plus dependencies (document-canvas-kit, entity-picker-kit, placeholder-kit, sortable-kit). Add path aliases, use `<qe-quotation-editor>` + `provideQuotationEditorKit()`.

See [README.md](./README.md), [COPY-GUIDE.md](./COPY-GUIDE.md).
